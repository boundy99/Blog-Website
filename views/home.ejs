<%- include('partials/header.ejs') -%>
    </header>
    <h1>Home</h1>
    <p>
        <%= paragraph %>
    </p>
    <% post.forEach(function(text){ %>
        <h1>
            <%= text.title %>
        </h1>
        <p>
            <%= text.body.substring(0,100) + "..." %>
                <a href="/posts/<%= text.title %>"> Read More</a>
        </p>

        <% })  %>

            <%- include('partials/footer.ejs') -%>